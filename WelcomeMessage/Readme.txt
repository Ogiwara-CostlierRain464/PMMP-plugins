1.1 getNameに成功。
1.2 Change Color
(TextFormat::RED .)で変更した
1.3 日本語に対応 UTF-8N,CRLR で保存したことにより可
1.4 英語に戻す。ブロードキャストメッセージ
の追加。また、""内で、$nameが使えることを確認。
1.5 メッセージの内容を代入することに成功。
また、\nが使えることに確認。ただし、改行すると色はリセットされてしまう。
1.6 コマンドを追加。/ruleと/tips
1.7 サブコマンドの追加。$rule1,2のように
分けることに。/rule check,set (setは未完成)
1.8 色文字のバグを直す。§は使えない。フォルダのままで読み込めることを確認。(devtoolのおかげ)
また、yml内での、β等の文字は不可。
*この時に、Configの導入成功。
1.9 configからの読み込みに成功。
2.0 大幅な変更,正式リリース

v1.1アイテムの付与
